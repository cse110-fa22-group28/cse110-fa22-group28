name: 'HTML Validator'
on:
 push:
   branches: [ CI_HTML_Valdation ]
 pull_request:
   branches: [ CI_HTML_Valdation ]
 
jobs: 
  build: 
   runs-on: ubuntu-latest  
  lint: 
     runs-on: ubuntu-latest      
    
     steps: 
     - uses: actions/checkout@v2
     - name: Lint Action
       uses: wearerequired/lint-action@v2.1.0
        
       env:
         GITHUB_TOKEN: ${{screts.GITHUB_TOKEN}}      
         OUTPUT_LOG: Reports 
         OUTPUT_DETAILS: detailed
         VALIDATE_ALL_CODEBASE: true
         
